# Data Manipulation & Wrangling
manipulation_pkgs <- c("tidyverse", "nanair")
install.packages(manipulation_pkgs)

# Data Visualization
visualization_pkgs <- c("ggplot2", "ggpubr", "sjplot", "ggthemes", "hrbrtheme", "ggsci", "viridis")
install.packages(visualization_pkgs)

# Summary Statistics
summary_pkgs <- c("gtsummary", "skimr")
install.packages(summary_pkgs)

# Specialized Analysis
analysis_pkgs <- c("likert", "car", "gapminder")
install.packages(analysis_pkgs)

# Advanced Tools
advanced_pkgs <- c("easystats", "devtools", "gt")
install.packages(advanced_pkgs)
